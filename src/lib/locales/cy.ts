// Welsh (Cymraeg) translations welcome — see CONTRIBUTING.md.
// Missing keys fall back to en-GB automatically. Add entries as
// `'hub.title': 'Her Mathemateg',` etc. Keep the keys identical to en-GB.
const cy: Record<string, string> = {};

export default cy;
