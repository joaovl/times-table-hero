// Spanish (Español) translations welcome — see CONTRIBUTING.md.
// Missing keys fall back to en-GB automatically. Add entries as
// `'hub.title': 'Reto de Matemáticas',` etc. Keep the keys identical to en-GB.
const es: Record<string, string> = {};

export default es;
